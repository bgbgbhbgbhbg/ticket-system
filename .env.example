# 這是範本檔案,實際使用時複製成 .env(去掉 .example)並填入真實密碼
# .env 本身絕對不能 commit 進 git(已在 .gitignore 排除),.env.example 這種範本檔案可以 commit,方便別人知道要填哪些變數
# 這是給docker-compose.yml用的,所以變數名稱要跟docker-compose.yml裡的對應起來
POSTGRES_PASSWORD=your_local_dev_password
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=your_local_dev_password
